# Instructions:
# 1. Rename this file to .env
# 2. Update all passwords and secrets with secure values
# 3. Never commit the actual .env file to Git

# Database Configuration
POSTGRES_DB=taskflow
POSTGRES_USER=admin
POSTGRES_PASSWORD=change_this_password_in_production

# Database Connection (for backend service)
DB_URL=jdbc:postgresql://localhost:5433/taskflow
DB_USERNAME=admin
DB_PASSWORD=change_this_password_in_production

# JWT Configuration
JWT_SECRET=change_this_secret_key_min_256_bits_for_production_use
JWT_EXPIRATION=86400000
